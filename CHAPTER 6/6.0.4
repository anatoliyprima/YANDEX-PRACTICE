6.0.4

Сквозное задание 4

* Укажите в unit-файле подключение к БД как опции при запуске java jar:
Скопировать код
BASH

# подставьте значения переменных
--spring.datasource.url=jdbc:postgresql://$PSQL_HOST:$PSQL_PORT/$PSQL_DBNAME
--spring.datasource.username=$PSQL_USER
--spring.datasource.password=$PSQL_PASSWORD

* Запустите сервис.


сервис запустился, однако появилось сообщение от бэкенда !
